# Self Hosting

Run the development stack:

```bash
docker compose up
```

Set Remembr and Ollama environment variables in your deployment system. Scale Remembr and Ollama independently; Mindr itself is a lightweight CLI/MCP process.
